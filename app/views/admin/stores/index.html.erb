<div>
  <h4>店舗一覧</h4>
  <p class="text-right"><%= link_to"新規登録", new_admin_store_path, class: "btn btn-success" %></p>
  <div class="store_explanation">
    <% @store.each do |store| %>
    <P><%= store.id %></P>
    <p><%= attachment_image_tag store, :image, size: "200x150" %></p>
    <p><%= store.name %></p>
    <% end %>
  </div>
  <div style="margin-left: 450px; margin-top: 50px;">
    <%= paginate @store %>
  </div>
</div>
