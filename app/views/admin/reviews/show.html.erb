<div>
  <h2>レビュー詳細</h2>
  <table>
     <%= link_to "レビュー削除", admin_review_path(@review), method: :delete %>
    <td>
      <% @review.review_images.each do |image| %>
        <%= attachment_image_tag image, :image, :fill, 150, 150 %>
      <% end %>
    </td>
    <td><%= @review.title %></td>
    <td>
      <% @review.scenes.each do |scene| %>
        <%= scene.name %>
      <% end %>
    </td>
    <td>
      <% @review.atmospheres.each do |atmosphere| %>
        <%= atmosphere.name %>
      <% end %>
    </td>
    <td><%= @review.text %></td>
  </table>
  <div>
    <p>コメント件数：<%= @review.review_comments.count %></p>
    <% @review.review_comments.each do |review_comment| %>
      <%= review_comment.user.nickname %>
      <%= review_comment.comment %>
      <%= review_comment.created_at.strftime('%Y/%m/%d') %>
      <div>
        <%= link_to "削除", admin_review_review_comment_path(review_comment.review, review_comment), method: :delete %>
      </div>
    <% end %>
  </div>
</div>
